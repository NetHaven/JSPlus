JSPlus is a library to add more classical object-oriented features to JavaScript. It supports the creation of class objects, fields, methods, 
public vs. private scope, constructors, a single-inheritance model, & static elements.

